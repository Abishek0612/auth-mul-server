version: '3.8'
services:
  mongodb:
    image: mongo:latest
    container_name: mongodb
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_ROOT_USERNAME: admin-user
      MONGO_INITDB_ROOT_PASSWORD: jdi23kd890
      MONGO_INITDB_DATABASE: auth-system
    volumes:
      - mongo-data:/data/db
volumes:
  mongo-data: